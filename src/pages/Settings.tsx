import React from "react";

interface SettingsProps {
  /**
   * The identifier of the component.
   */
  identifier?: string;
}

const Settings: React.FunctionComponent<SettingsProps> = ({ identifier }) => {
  return <>SETTINGS</>;
};

export default Settings;
