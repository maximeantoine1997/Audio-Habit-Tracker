import React from "react";

interface CalendarProps {
  /**
   * The identifier of the component.
   */
  identifier?: string;
}

const Calendar: React.FunctionComponent<CalendarProps> = ({ identifier }) => {
  return <>CALENDAR</>;
};

export default Calendar;
